/// <reference path="./../../includes.d.ts"/>

namespace com.vmware.library.microsoft.activeDirectory {
export function createUserGroup(groupName?: string, container?: any): void {container.createUserGroup(groupName);
}
}
