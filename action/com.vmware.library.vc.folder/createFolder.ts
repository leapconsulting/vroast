/// <reference path="./../../includes.d.ts"/>

namespace com.vmware.library.vc.folder {
export function createFolder(parentFolder?: any, name?: string): any {return parentFolder.createFolder(name);
}
}
